# GitHub Personal Access Tokens (public read is enough)
# https://github.com/settings/tokens
#
# Option A — single token (backward compatible):
GITHUB_TOKEN=ghp_your_token_here
#
# Option B — multiple tokens (auto-rotate when one is rate-limited).
# Comma-separated or one per line:
# GITHUB_TOKENS=ghp_token_one,ghp_token_two,ghp_token_three
#
# Option C — numbered tokens:
# GITHUB_TOKEN_1=ghp_token_one
# GITHUB_TOKEN_2=ghp_token_two
# GITHUB_TOKEN_3=ghp_token_three
#
# You can combine A+B+C; duplicates are ignored.

# Server port (default: 3000)
PORT=3000

# Frontend origin for CORS (default: http://localhost:5173)
CORS_ORIGIN=http://localhost:5173

# In-memory cache TTL in seconds (default: 300)
CACHE_TTL_SECONDS=300

# Team collaboration auth (change in production)
AUTH_SECRET=your-random-secret-here

# API base URL for OAuth redirect callbacks
API_BASE_URL=http://localhost:3000

# Integration OAuth (optional — demo mode works without these)
# GITHUB_OAUTH_CLIENT_ID=
# GITHUB_OAUTH_CLIENT_SECRET=
# LINKEDIN_OAUTH_CLIENT_ID=
# LINKEDIN_OAUTH_CLIENT_SECRET=
# GREENHOUSE_OAUTH_CLIENT_ID=
# GREENHOUSE_OAUTH_CLIENT_SECRET=
# LEVER_OAUTH_CLIENT_ID=
# LEVER_OAUTH_CLIENT_SECRET=
# SLACK_OAUTH_CLIENT_ID=
# SLACK_OAUTH_CLIENT_SECRET=
# GMAIL_OAUTH_CLIENT_ID=
# GMAIL_OAUTH_CLIENT_SECRET=
# OUTLOOK_OAUTH_CLIENT_ID=
# OUTLOOK_OAUTH_CLIENT_SECRET=
